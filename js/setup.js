export function welcomeView(hasSave,version){return`<section class="hero"><div class="hero-copy"><p class="eyebrow">Draft the impossible. Simulate the season.</p><h1>Build your<br><em>Legends</em></h1><p class="lede">Spin through every franchise and era, assemble a five-player dream team, then test it across a complete 82-game season.</p><div class="actions">${hasSave?'<button id="resume" class="button">Resume saved game</button>':''}<button id="start" class="button ${hasSave?'secondary':''}">Start new game</button></div><p class="footer-note">Dataset ${version.datasetVersion} · ${version.currentSeasonLabel} · local saves only</p></div><aside class="card hero-card"><p class="eyebrow">Your season</p><h2>Five spins from glory.</h2><p class="lede">Every pick is position-valid. Every new game brings fresh outcomes, while autosave keeps the season consistent.</p><div class="stat-strip"><div class="stat"><strong>30+</strong><span>Franchises</span></div><div class="stat"><strong>82</strong><span>Games each</span></div><div class="stat"><strong>1–8</strong><span>Players</span></div></div></aside></section>`}
export function setupView(){return`<div class="screen-head"><div><p class="eyebrow">Game setup</p><h1>Choose your<br><em>contenders</em></h1></div></div><form id="setup-form" class="card stack"><label>Number of human players<input id="user-count" type="number" min="1" max="8" value="1" required></label><div id="names" class="form-grid"></div><label>Default simulation speed<select id="speed"><option>Slow</option><option selected>Normal</option><option>Fast</option><option>Instant</option></select></label><label class="check"><input id="strict" type="checkbox"> Strict player uniqueness across custom teams</label><label class="check"><input id="reduced" type="checkbox"> Reduce wheel and progress motion</label><div class="actions"><button class="button" type="submit">Set draft order</button></div></form>`}
import{escapeHTML}from'./utils.js';
export function nameFields(count,values=[]){return Array.from({length:count},(_,i)=>`<div class="card"><label>Player ${i+1} name<input name="user-name" maxlength="30" value="${escapeHTML(values[i]?.name||'')}" required></label><label>Custom team name<input name="team-name" maxlength="40" value="${escapeHTML(values[i]?.teamName||'')}" placeholder="Suggested after typing" required></label></div>`).join('')}
